# A convenience file which can be sourced to set connection variables for the
# `vault` CLI to connect to the server started by the `dev/server` script.

export VAULT_ADDR="http://127.0.0.1:8200"
export VAULT_TOKEN="t0p-53cr3t"
